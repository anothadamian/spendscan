export const categories = ['Makan & minum', 'Belanja', 'Transportasi', 'Tagihan', 'Hiburan', 'Lainnya'] as const;
export type Expense = { id: string; merchant: string; amount: number; category: string; date: string };
export const money = (n: number) => new Intl.NumberFormat('id-ID', {style:'currency',currency:'IDR', maximumFractionDigits:0}).format(n);
export const localDate = (d = new Date()) => `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;
export function validExpense(x: unknown): x is Expense { if (!x || typeof x !== 'object') return false; const e=x as Expense; return typeof e.id==='string' && typeof e.merchant==='string' && Number.isFinite(e.amount) && e.amount>0 && categories.includes(e.category as typeof categories[number]) && /^\d{4}-\d{2}-\d{2}$/.test(e.date); }
export function demoExpenses(today: string): Expense[] { const month=today.slice(0,7); return [['Kopi Tuku',28000,0,17],['Grab',24500,2,17],['Super Indo',186500,1,16],['Netflix',65000,4,15],['Ayam Geprek Pak Gembus',32000,0,14],['PLN',250000,3,12],['Uniqlo',399000,1,10],['Fore Coffee',42000,0,9],['Bensin',100000,2,7],['Makan siang',38000,0,5],['Internet rumah',299000,3,3],['Belanja bulanan',276000,1,1]].map(([merchant,amount,cat,day],i)=>({id:`demo-${i}`,merchant:String(merchant),amount:Number(amount),category:categories[Number(cat)],date:`${month}-${String(Math.min(Number(day),Number(today.slice(-2)))).padStart(2,'0')}`})); }
export function streak(expenses: Expense[], today: string) { const days=new Set(expenses.map(e=>e.date)); const d=new Date(today+'T12:00:00'); if(!days.has(localDate(d))) d.setDate(d.getDate()-1); let n=0; while(days.has(localDate(d))) { n++; d.setDate(d.getDate()-1); } return n; }
